services:
  arch-copilot:
    container_name: arch-copilot-app
    build: .
    ports:
      - "7070:7070"
    environment:
      - PORT=7070
      - UI_PORT=7070
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
