services:
  arch-copilot:
    container_name: arch-copilot-app
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - UI_PORT=8080
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
